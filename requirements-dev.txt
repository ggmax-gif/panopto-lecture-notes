# Running the tests. The suite needs neither ffmpeg, yt-dlp, Vision nor a
# model server — OCR is stubbed and subprocess calls are faked — so this is
# all of it. Nothing here is needed to *run* lecturescrape; see requirements.txt.
pytest
pillow          # draws the slide frames the blank-frame tests judge
